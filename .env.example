# Vercel Blob (server-side)
# Em produção, a Vercel injeta automaticamente quando o Blob está conectado ao projeto.
BLOB_READ_WRITE_TOKEN=

# Vercel Postgres (server-side)
# Em produção, a Vercel injeta automaticamente ao conectar Postgres/Neon no projeto.
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=
